# Build Resources

Repository-wide build/release resources belong here when introduced by later implementation issues. Issue 1 keeps desktop-specific placeholder branding under `apps/desktop/build/` and deliberately does not add installer/signing logic yet.
