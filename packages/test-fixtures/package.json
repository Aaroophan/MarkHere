{
  "name": "@markhere/test-fixtures",
  "version": "0.1.0-alpha.0",
  "private": true,
  "type": "module",
  "description": "Deterministic shared Markdown/security test fixtures",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  }
}
